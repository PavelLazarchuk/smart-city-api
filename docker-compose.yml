services:
    api:
        build:
            context: .
            target: dev
        env_file: .env
        environment:
            NODE_ENV: development
            MAIL_PROVIDER: smtp
            SMTP_HOST: mailhog
            SMTP_PORT: 1025
            SMTP_SECURE: 'false'
            REDIS_URL: redis://redis:6379
        ports:
            - '8080:8080'
        volumes:
            - .:/app
            - /app/node_modules
        depends_on:
            mailhog:
                condition: service_started
            redis:
                condition: service_healthy
            mongo:
                condition: service_healthy
                required: false

    mailhog:
        image: mailhog/mailhog:v1.0.1
        ports:
            - '1025:1025'
            - '8025:8025'

    redis:
        image: redis:8.2-alpine
        command:
            [
                'redis-server',
                '--save',
                '',
                '--appendonly',
                'no',
                '--maxmemory',
                '256mb',
                '--maxmemory-policy',
                'allkeys-lru',
            ]
        ports:
            - '6379:6379'
        healthcheck:
            test: ['CMD', 'redis-cli', 'ping']
            interval: 5s
            timeout: 3s
            retries: 10
            start_period: 5s

    # Optional local database: `docker compose --profile local-db up`.
    mongo:
        image: mongo:8.2
        profiles: ['local-db']
        command: ['--replSet', 'rs0', '--bind_ip_all']
        ports:
            - '27017:27017'
        volumes:
            - mongo-data:/data/db
        healthcheck:
            test:
                [
                    'CMD-SHELL',
                    'mongosh --quiet --eval "try { rs.status().ok } catch (e) { rs.initiate({ _id: \"rs0\", members: [{ _id: 0, host: \"mongo:27017\" }] }).ok }" | grep -q 1',
                ]
            interval: 5s
            timeout: 5s
            retries: 20
            start_period: 10s

volumes:
    mongo-data:
